%load char~();;
~:pack = %load rbbpack

%lit:
  trigger = '",
  eval = ~fn~ (|stream) {
    ~:strdata = ~pack (|)

    ~stream skip
    ~while~ {!~stream char_val /= '"} {
      ~:strdata = ~strdata concat (~pack|~stream char_val)
      ~stream skip
    }

    !~strdata
  }
