~:putch = %load rbbputch
%load char~();;

~:puts = ~fn~ str {
  ~use_assert~puts
  ~assert|~str has_iface string, expecting_string, str, :got = ~str

  ~foreach~ (~str) ch {
    ~putch (~ch)
  }

  ~putch '\n'
}

~:print = ~fn~ obj {
  ~puts (~obj_to_string (~obj))
}

